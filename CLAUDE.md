# CLAUDE.md — proto-controle-technique

> **Important** : ce fichier ne réécrit pas la doc du projet. Avant toute tâche, **lire d'abord** les docs listées ci-dessous.

## 📚 Documentation à lire en priorité

- Fiche vault : `~/Documents/Obsidian/10-Projects/proto-controle-technique.md`

## 🧰 Indices de stack

Fichiers de build/config détectés à la racine : **Node/JS, Docker, Docker Compose**.

Les détails (versions, dépendances, scripts) sont dans la doc du projet. Ne pas les dupliquer ici.

## 🗄️ Contexte Obsidian

- Fiche projet dans le vault : `~/Documents/Obsidian/10-Projects/proto-controle-technique.md`
- ADR historiques : `~/Documents/Obsidian/30-Knowledge/ADR/` (filtrer par `project: proto-controle-technique`)
- Sessions précédentes : `~/Documents/Obsidian/20-Sessions/` (frontmatter `projects:` contient `proto-controle-technique`)

## ✅ Règles Claude-specific (en plus de la doc du projet)

1. **Toujours lire la doc listée ci-dessus avant d'agir**. Ne pas supposer la stack, ne pas deviner les commandes.
2. **Décision structurante** → `/new-adr "<titre>"`.
3. **Fin de session significative** → `/session-end`.
4. **Pas de commit direct sur `main`**, pas de `git push --force`, pas de modification des `.env*`.
5. **Pas d'install de dépendance nouvelle** sans m'en parler d'abord.
6. Doc obsolète ou manquante sur un point → **le signaler** et proposer de la mettre à jour.

## 📏 Règle d'or

Ce fichier doit rester **sous 80 lignes**. Son unique rôle : pointer vers la vraie doc et rappeler les règles Claude.

<!-- Auto-généré le 2026-04-11 — à relire et affiner. -->
